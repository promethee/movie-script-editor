# docs/

Assets referenced by the project's README.md.

- `screenshot.png` — main app screenshot (Write or Split view), used in the top of README.md
- Optional: `demo.gif` — short screen recording (typing → preview → export) for a richer README hero
